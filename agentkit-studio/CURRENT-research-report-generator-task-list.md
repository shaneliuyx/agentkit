# Current Research Report Generator Task List

Source files:

- `WORKLOG-research-report-generator-plan.md`
- `PLAN-research-report-skill-package-improvements.md`

## Completed

- Generic report-generator guardrail added: source code stays task-neutral; generated reports must be task/evidence-specific.
- Evidence matrix first slice implemented: `EvidenceItem`, evidence SSE event, frontend display, export/run snapshot persistence.
- Publish/readiness gate first slice implemented: detects missing citations, short reports, request-term misses, lint failures, and review-required state.
- Unified scorecard/scoring matrix core implemented: frozen profile/template scoring matrix, profile-related category filtering, scorecard display, worker scoring cropping, reducer/final full scoring requirements.
- Final synthesis prompt handoff fixed and regression-tested with:
  - `FULL SCORING STANDARD`
  - `UNRESOLVED WEAKNESSES`
  - `FETCHED EVIDENCE FILES`
  - `evidence/fetched-sources.json`
  - `evidence/source-001.md`
- Empty final-synthesis scoring fallback fixed: final synthesis falls back to the profile/template default matrix instead of `- (none for this assigned section)`.
- Fetched evidence is passed to final synthesis by workspace file path, not raw cache content.
- Seeded and cold final-step regressions added, including generated `io/*.in.md` prompt-file inspection.
- Weak-model guardrails partially implemented: model profiles, Gemma action/search/fetch budgets, planner prompt heuristics, and no automatic model-id workflow override.
- Section ownership, assignment queue, and reducer patch-schema work implemented in earlier slices.
- Report-quality lints expanded: duplicate headings, placeholders, malformed links, citation walls, orphaned code, citation-free long sections, table/diagram/code static checks.
- Research package/export first slices implemented: markdown, HTML, evidence matrix, scorecard, review checklist, manifest, metrics, trace/checkpoints JSONL, source notes, demo files.
- Metrics/stop report implemented: metrics SSE event, stop reason, tool failures, review state, scorecard/evidence counters.
- Trace/checkpoint JSONL implemented with redacted tool args and pre-validation checkpoint.
- Template catalog backend implemented: audit/quarantine, inventory, replace, import/export, approval metadata/action.
- Repeated finding dedupe implemented before patch generation.
- Latest validation recorded in worklog entry 152:
  - focused final prompt/fallback tests: 5 passed
  - `backend/tests/test_runner.py`: 87 passed
  - comparable backend slice: 239 passed
  - full backend suite: 498 passed

## Completed — this session (2026-07-01)

- Reducer-side finding consolidation (`agentkit/artifacts/dedup.py::consolidate_findings`): same-URL merge (keep richest), per-`patch_target` density cap, scaffolding-lead strip; against-doc dedup wired in `findings.py` reduce(). Live cold run showed `url_merged=7` (13 raw findings → 6 distinct patches). Thins the citation wall at the source.
- artifact/result split (`runner.py` post-gate finalize): `artifact.md` = readable+deduped seed (scored + carried forward), `result.md` = grounded full archive. So per-round cleanup compounds instead of being re-paid.
- Per-section readability refine (`artifact_text.py::_refine_readability` + `_split_top_level`): windows by top-level `##` section so the model synthesizes the whole section (bigger picture) instead of per-paragraph fragments that fail the citation guard.
- Reducer now receives FULL SCORING STANDARD + UNRESOLVED WEAKNESSES + FETCHED EVIDENCE FILES each phase (`findings.py::_make_section_reducer` + call site). Fixed the reducer's empty-scoring bug (used raw `rubric_config.scoring_matrix`; now `_full_scoring_matrix(session)` with profile/template fallback — the same gap entry-152 fixed for final synthesis). Live proof: `io/e1:s1.reducer.in.md` has all three (18 fetched sources).
- Reducer prompt+output now persisted to `io/<step>.reducer.in.md`/`.out.md` (was invisible — the reducer runs inside `run_plan`, not as a recorded spoke).
- Verified entry-152 fix live: `io/e1:s16.in.md` (final synthesis) has non-empty FULL SCORING STANDARD (9 rows via fallback) + UNRESOLVED WEAKNESSES (15 section-prefixed rows).
- Workstream I (loop first): local `research-report-agent` loop (`backend/studio/local_catalog/loops/research-report-agent.json`) merged into `CatalogClient` via `include_local` — discoverable in `/loops`, seedable via `/session/{id}/seed`; local overrides remote by slug and survives remote-fetch failure. Domain skill `research-report-agent` (`skills_paths.py::build_domain_skills`) exposed through `/skills`.
- Loop-seed carry-forward via semantic search (local + remote loops): when a seeded/reworded run rotates the task_hash and the exact-key prior lookup misses, the runner falls back to `TaskRunStore.similar_runs` (cosine over requirement embeddings, min 0.6) and seeds from a similar prior's artifact. Selection: no DB match above threshold -> cold start; skip empty/placeholder priors; when several qualify choose the latest (`session_recency` = max row id). Live: loop-seeded run seeded the 0.92 prior artifact (21919 bytes) instead of cold-starting ~8K. `runner._pick_seed_with_content` + `TaskRunStore.session_recency`, unit-tested; full suite 506 passed.
- Validation: full backend suite green after all the above (506 passed); new tests in `test_section_ownership.py` (reducer injection + io_capture, consolidate_findings), `test_loops.py` (local loop find/seed/override), `test_skills_paths.py` (domain skill).
- Explicit seed-file parameter (`hill_climb_config.seed_path`) + GUI: point any run at a chosen artifact on disk, overriding both exact-hash and semantic DB seeding — the escape hatch when a weak same-task lineage (e.g. the ~8K catalog lineage) blocks a stronger 22K seed. `runner._seed_prior_from_path` builds a synthetic prior (file body → `result_text`), runs before DB lookup; `/session/{id}/hill-climb` + `LoopConfigPanel` "Seed file" input plumb it. Live-proven: throughput log `seed via explicit seed_path (21899 chars)` beat the 7988-char DB prior. Tests: `test_seed_prior_from_path_reads_file_and_falls_back`; `tsc` clean.

## Completed — this session (2026-07-02)

- Seed-path re-seed guard: `_seed_prior_from_path` (the `seed_path` override) now applies only on `self._epoch <= 1`, so a multi-epoch hill-climb no longer discards its own epoch-2+ progress by re-seeding the static file every epoch. Test: `test_seed_path_only_seeds_first_epoch_not_every_epoch`.
- Goal-aware editor phase (`runner.py::_run_editor_pass`, ≤2 rounds, full revert on any regression, offers only `read_file`/`search_evidence`/`read_artifact`/`patch_artifact`) plus the new `search_evidence` tool (greps `evidence/*.md`, capped matches/context, skips the `.json` manifest). Real-gemma verified ACCEPT case: score 0.920 → 1.000 fixing an orphaned-code-block lint defect on `s_db8f2ec3b920`. Also observed two legitimate real-gemma REVERTs on citation-integrity weaknesses on the same fixture (flat 0.718→0.718 both rounds) — the revert-on-regression safety working correctly.
- Worker-side `search_evidence` wiring tested and NOT shipped: no measurable score improvement, and gemma ignored real search hits in favor of re-fetching when writing new content as a worker — a model-behavior ceiling, independently confirmed via a counterfactual after the tool bugs below were patched. Do not re-test; settled.
- `search_evidence` bug fix: Bug A (contiguous-substring match missed multi-word queries like `"methodology and structure"` when the phrase spanned lines/words — fixed with tokenized majority-match); Bug B (hits had no source URL — fixed by parsing the `URL:` header each `evidence/source-NNN.md` already carries and attaching it to every match). 3 new tests in `test_tools.py`.
- Retest of the citation-integrity REVERT case with the fix live (`s_db8f2ec3b920`): `search_evidence` confirmed working (multi-word queries now return hits with correct urls), but the editor STILL reverted both rounds on the same citation-integrity weaknesses (flat 0.718→0.718). Genuine negative result: this model's citation-integrity ceiling on this fixture is real, independent of the tool bugs just fixed.
- Full backend suite: 520 passed, no regressions.
- Documented (not built here — an earlier undocumented pass this session): editor tool allowlist widened to 6 tools (`edit_file`/`glob` added to `read_file`/`search_evidence`/`read_artifact`/`patch_artifact`); `edit_file`'s studio-local cap raised 64→100 KiB (`studio/tools.py::_EDIT_FILE_MAX_BYTES`), threaded through both `Workspace.read(max_bytes=...)` and the write-side length check.
- Cross-task R10 seeding contamination root-caused: `s_98742f3026ee` ("benchmarking coding agents") seeded via R10 (sim=0.957) from a different-topic prior (`s_f2c641b4a7d2`, "catalog management for agent loops"); seeded sections kept catalog-specific citations. Honest nuance: the two requirements' 0.948 full-text cosine is partly self-inflicted by shared test-driver boilerplate framing, not purely topical coincidence — fix is adaptation, not threshold-retuning.
- Cosine relevance detector calibrated against the real fixture and REJECTED before shipping: contaminated content scored HIGHER against the wrong requirement (0.887) than its own (0.869) — wrong direction, at every granularity (section/sentence/topic-phrase). A synthetic control confirmed cosine only separates far-domain content; same-domain drift (the R10 case by construction) is undetectable. Escalated instead of shipping an inert feature.
- Binary LLM relevance check built instead (`studio/relevance.py::relevance_issues`, new): narrow single-section-vs-task binary classification, run ONCE per section per epoch, gated on a new `_seed_cross_task` flag (`_seed_carry_forward` now returns `_seed_via_similarity`), NEVER inside `rubric_score`/`rubric_scorecard_100` (those are hot — called ~10+ times per epoch). Prompt calibration against the real fixture (local qwen-14B via oMLX): naive phrasing flagged 0/8 real contaminated sections (same surface-vocabulary-bias failure mode as cosine); an explicit "SPECIFIC topic, not adjacent" phrasing recovered 1/8 — non-zero, a real improvement, but a genuine recall ceiling with this local model (documented in the module docstring, upgrade path = swap in a stronger client). A CoT-lite "extract subject, then compare" variant was tried and made it WORSE (0/8, self-anchoring) — reverted.
- `rubric_score`/`rubric_scorecard_100` gained an optional `relevance_penalty: float = 0.0` kwarg (pure/deterministic, no I/O — the LLM call happens once upstream and is threaded in as a plain float). Editor gained a 4th action (relevance issues pure-union into the existing rubric+lint weakness list feeding the ≤2-round revert-on-regression loop, mechanics unchanged). Worker gained a relevance repair-clause mirroring the existing lint repair-clause pattern exactly (narrow "replace, don't just append" exception, gated on `_seed_cross_task`).
- Synthesis/contradiction-reconciliation prompt guidance added (prompt-only, no new detector — `_citation_wall_issues` was already flowing into both consumer prompts via `lint_artifact`; the gap was the generic fix-instruction). Bounded real-gemma calibration (2 scenarios: quote-wall, genuine numeric contradiction; 3 candidate instruction variants: terse-imperative, explanatory-with-example, checklist-style). Terse-imperative DISQUALIFIED on real output — it caused gemma to DROP verbatim quotes while paraphrasing, a grounding-loss regression risk. Explanatory and checklist both preserved quotes AND added real synthesis AND correctly reconciled the contradiction; checklist's semantics (adopted) were marginally clearest. The decomposed-turns contingency was not triggered (combined single-turn instruction worked on real output) so it was not additionally tested. Shipped in `_build_executor_prompt`'s WHY: field (worker) and `_EDITOR_PERSONA` (editor).
- New tests: `tests/test_relevance.py` (5 synthetic + 1 real-data `@pytest.mark.integration` calibration test), `tests/test_rubric.py` (+4), `tests/test_runner.py` (+3, including a cross-task worker-repair-clause E2E test mirroring the existing mermaid-repair test).
- Full backend suite: 547 passed, 2 deselected (was 520); `-m integration` -> 1 passed (new), 1 skipped (pre-existing, unrelated quirk).
- Two hill-climb anti-regression correctness fixes in `runner.py`:
  - **Keep/discard gate baseline fix (`_run_phase_loop`)**: the last-phase seed block rebound the epoch-seed `_seed_text` to the current on-disk `artifact.md` (which by the last phase already holds THIS epoch's own folded findings), so `epoch_gate.accept_epoch` compared the epoch against itself and could never reject a regression. The on-disk read is now a local `_seed_on_disk` used only by the lint/relevance repair clauses; the epoch seed flows to the gate untouched.
  - **Post-loop guard unified on `accept_rewrite`**: the post-loop patch-apply path replaced its crude `len(new) >= _seed_len` length floor with the same section-granular `accept_rewrite` guard the per-phase writeback already uses (+ matching `writeback ACCEPT/REJECT` `_dbg` logging), so a legitimately shorter-but-better merge (dedup / synthesis) is kept instead of rejected for being shorter — one consistent anti-regression mechanism.
  - Tests: `test_epoch_gate_baseline_is_the_seed_not_this_epochs_own_output` (full seeded run; captures the gate's `prior_text`, asserts it is the seed not the fresh output — verified to fail when the clobber is reintroduced) and `test_postloop_guard_accepts_shorter_but_improved_rewrite` (shorter dedup rewrite accepted by `accept_rewrite` where the old length floor rejects it; gutted section still rejected). Full backend suite: 549 passed, 2 deselected.
- Relevance-check prompt upgrade + model-conflation correction (`studio/relevance.py`, `studio/runner.py`, tests):
  - **Correction — the prior calibration record was wrong about the model.** Entry 157's docstring claimed a "1/8 recall ceiling on gemma". That number was actually `qwen` (Qwen2.5-Coder-14B, a fallback), NOT the deployed model. On the ACTUAL deployed `gemma-4-26B-A4B-it-heretic-4bit` (`gemma` profile), even the naive baseline already scores 7/8 recall on the same real fixture. Docstring rewritten to state this explicitly; the false "1/8 on gemma" claim removed.
  - **Winning prompt wired in** (verbatim, calibration-tested): dynamic-exemplar few-shot + evidence-extraction, `QUOTE:`/`VERDICT: RELEVANT|IRRELEVANT` format, `VERDICT:` regex parse, fail-open on unparseable. Real gemma numbers: 7/8 recall, 1/8 false-flag (beats few-shot-alone 2/8 false-flag, evidence-alone 3/8 recall, static-exemplar 5/8 recall).
  - **References-skip**: pure references/bibliography sections (heading in references/sources/bibliography/works cited/citations) are skipped — they have no topical sentence to quote and were the single residual false-flag. Simple heading check, not a classifier.
  - **`seed_topic` threading**: `relevance_issues` gained a `seed_topic` param; `runner._seed_carry_forward` now captures the cross-task R10 seed's own `requirement` and threads it (return tuple 7→8, through `_run_phase_loop`) so EXAMPLE A names the seed's real origin subject; generic-phrase fallback when no seed provenance.
  - Tests: `test_relevance.py` +3 unit (References-skip, `seed_topic` exemplar + fallback, unparseable fail-open); integration test switched to `gemma` profile + `seed_topic`, asserts majority-flagged (`penalty >= 0.5`, corrected from old 1/8). `test_runner.py` cross-task-repair mock updated to the `VERDICT:` contract. Full suite: 552 passed, 2 deselected; `-m integration` 1 passed (live gemma). Live production-path sanity on the real fixture: penalty=0.8571 (6/7 flagged, References skipped).
- Self-propagating-contamination finding + two defense-in-depth fixes (see WORKLOG entry 160). Live run `s_38360e413cdc` R10-seeded from `s_98742f3026ee` — itself the original earlier-session contamination case, recorded to `task_runs.db` BEFORE the relevance-check feature existed, so its score never had a relevance penalty and it looked clean enough to be reselected as a seed, propagating its contamination. The classifier miss is the known ~85% recall ceiling (7/8), not a new bug — accepted, out of scope.
  - **Fix 1 — unconditional cross-task adaptation notice** (`runner.py::_run_phase_loop`): when `_seed_cross_task` is True, an always-on `CROSS-TASK SEED` instruction is injected into the worker/reducer prompt regardless of whether the LLM classifier flagged anything — the detection-independent backup that stacks with the existing conditional repair-clause. No new LLM call. Wording tightened (this + the existing conditional clause) from soft "replace/adapt" to explicit "DROP the off-topic content entirely, then WRITE NEW content addressing the current task" (soft wording let the model just reword the same off-topic substance).
  - **Fix 2 — deprioritize pre-relevance-check R10 seeds** (`task_runs.py`): new `TaskRun.relevance_checked` field + `relevance_checked` DB column (migration defaults existing rows, incl. `s_98742f3026ee`, to 0 = not-checked); set True only when `relevance_issues(...)` ran that epoch. `similar_runs()` applies a 0.15 rank penalty to unchecked candidates — deprioritize, not exclude (most legitimate runs are also unchecked since the check only runs on cross-task-seeded epochs). Additive schema + ranking only; no existing rows modified, no retroactive re-scoring.
  - Honest note: the ~85% classifier recall ceiling is a known, accepted limitation. These fixes are defense-in-depth + propagation-prevention, NOT a claim that per-section detection is now perfect.
  - Tests: `test_runner.py` +1 (`test_cross_task_seed_injects_unconditional_adaptation_notice`; existing cross-task-repair test updated to the tightened wording), `test_similar_runs.py` +4 (flag round-trip, unchecked deprioritized-but-present, checked selected normally, legacy rows backfill to unchecked). Full suite: **557 passed**, 2 deselected (was 552).
- Coarse whole-document seed-relevance gate (`studio/relevance.py::seed_doc_relevance`, entry 161): ONE LLM call at seed-resolution time, BEFORE epoch 1, gated on `_seed_via_similarity` (cross-task R10 seeds only — same-task lineage never pays for it). Summarizes the whole seed doc and the task, drops the seed on a NOT_RELATED verdict (falls back to the cold-start blank template), fail-open on any error. Calibrated on 10 real fixtures × 3 samples = 30 real gemma samples: 100% recall on cross-field mismatch, 0/6 false-reject. ADDITIVE to the per-section `relevance_issues()` check, not a replacement — the per-section check still runs every epoch regardless of this gate's verdict. Tests: `test_relevance.py` +5. Full suite: 562 passed, 2 deselected (was 557). NOT yet e2e-validated against a live cross-field-seed run as of this entry.
- Duplicate-title-at-two-heading-levels fix (`studio/section_workspace.py::_normalize_heading_levels`, entry 162): root-caused on real live evidence (session `s_089481ef5161` — every section title appeared twice, once at `#` and once at `##`). `split_sections` keys only on `##`, so a reducer that emitted a full standalone report at `#` H1 got parsed as preamble while the outline still generated a thin `##` placeholder. Fix normalizes any heading matching a known outline title to `##` before `split_sections` runs, with richest-body-wins dedup so the reducer's real synthesis is preserved and supersedes the placeholder rather than being discarded. Real-fixture regression test (`tests/fixtures/reducer_h1_dump.md`, verbatim bytes from the live session). Full suite: 563 passed, 2 deselected (was 562).
- Post-run markdown beautification (`studio/markdown_format.py::beautify_markdown`, entry 163): `mdformat` + `mdformat-gfm` (pinned versions), applied ONLY at the final return of `_postrun_score_and_record` — after all scoring/gating/DB-recording has run on the unformatted text, so only the served copy is cosmetically normalized (heading spacing, list markers, table alignment). Fail-open on formatter error. Full suite: 566 passed, 2 deselected (was 563).
- `_TITLE_STOP_RE` too-greedy title truncation fix (`studio/artifact_text.py`, entry 164): real live evidence (`s_9b7bacfdc703`) showed the Pi/Craft report title truncated to `# Study how to` because the old regex matched the FIRST occurrence of a stop word (`use`, `include`, etc.) anywhere in the sentence, not just a trailing instruction clause. Fixed by comma-anchoring the match (`,\s*(?:\w+\s+){0,3}\b(?:include|use|cover|provide|with)\b.*$`) so it only strips genuine trailing instructions. Regression test with the real requirement text. Full suite: 567 passed, 2 deselected (was 566).
- Independent Codex CLI adversarial review (`/codex challenge` against the full branch diff) — 6 production-risk findings, all fixed with real code + regression tests (entry 165, 2026-07-02):
  1. **CRITICAL** — `/catalog/templates/{audit,replace,approve,export,import}` had no auth, and `/replace` left a stale `requirement_embedding` enabling persistent cross-task contamination via a poisoned template. Fixed: opt-in `X-Studio-Admin-Key` header auth (`studio/app.py::require_catalog_admin`, `hmac.compare_digest`, keyless-dev-default preserved when `STUDIO_ADMIN_KEY` unset) on the 5 mutation/export routes; `replace_template` now recomputes or clears the embedding.
  2. **HIGH** — `agentkit/topology/dynamic.py` used module globals (`_POOL_WORKERS`/`_MAX_SPOKES`/`_REDUCER`) for per-run state, corrupting concurrent sessions (Studio runs each session on its own thread). Fixed: converted to `contextvars.ContextVar`, set/reset per `run_plan` call.
  3. **HIGH** — race-prone version allocation in `task_runs.py` (`next_version()` then `record()` as two steps could let two concurrent auto-improve runs insert the same version). Fixed: `UNIQUE(task_hash, version)` index + `TaskRunStore.record_versioned()` with retry-on-conflict.
  4. **HIGH** — a process-global fetch cache let one session "verify" citations via another session's fetches. Fixed: `_ContextFetchCache`, a `contextvars`-backed per-thread cache with the same dict interface (no call-site changes needed at ~50 usages).
  5. **HIGH** — `last_run` was published (raw) before postrun URL-neutralization/editor-pass finished, letting `/chat`/`/export` serve stale unvalidated output. Fixed: removed the premature `record_run` call.
  6. **MEDIUM** — fail-open URL verification couldn't distinguish "no citations" from "couldn't check during an outage," silently masking fabrication. Fixed: `_web_cache_available()` distinguishes the two cases and emits a warning on the ambiguous one, while preserving the deliberate fail-open (an outage must never blank real citations).
  - Tests: `test_codex_findings.py` (new, one per finding) + `test_dynamic_topology_concurrency.py` (new). Full suite: **575 passed**, 2 deselected (was 567); agentkit lib: **433 passed** (+1). NOT committed.

## In Progress / Partially Complete

- Live E2E validation after the latest final-synthesis fixes is still pending. Unit/regression tests prove prompt-file contents, but the fresh live comparable run did not reach normal `StepRun` completion.
- The missing `io/` subdir behavior is understood: `io/` is created only for normal step diagnostic files; runs that only record run-summary/assignment queue will not create it.
- Report quality is still not proven good enough in live output. Previous failures included shallow analysis, weak summary/reflection, poor use of fetched documents, and missing detailed synthesis.
- Weakness lifecycle is partially implemented, but needs more live validation: reducers generate current-stage weaknesses; achieved scoring rows should be removed from later worker prompts; reducer/final scoring still uses full requirements.
- Evidence-yield quality is still under active improvement: the current path preserves and passes fetched files, but live runs still need comparison against better prior workspaces such as `s_ab00296748df` / `s_831049d2caee`.
- Workstream C is implemented in core behavior, but broader profile/template scoring correctness needs more E2E drift validation.
- Workstreams K/L/M are partially complete: JSONL trace, checkpoints, metrics, HTML/demo export are done; richer indexed/queryable observability remains open.

## Deferred

- SQLite indexing for observations/checkpoints; current plan uses JSONL first.
- PDF/PNG rendering and richer static diagram export; current bundle marks these unavailable instead of blocking export.
- Manual human approval workflow; current state exposes review-required/checklist but does not implement approval queues.
- Full trace-derived metrics such as recovery rate and validation catch rate from a complete observation model.
- Remote skill catalog schema and remote skill source support.
- Richer frontend/editor workflows for template approval and catalog management.
- Optional sandboxed execution of generated code blocks; current checks are static.
- Vector/RAG/tree-index dependency work for large uploaded documents or semantic source/catalog retrieval.
- [LOGGED, NOT FIXED — deferred per explicit user choice given session cost] Run-death-loses-all-progress on sustained LLM backend outage (WORKLOG entry 166): the single top-level `except Exception` in `runner.py` treats an exhausted-retry-budget `LLMUnavailable` (7 retries/~4min) as fully session-fatal — nothing is recorded to `task_runs.db`, no partial artifact, no resumability, even after hundreds of thousands of tokens' worth of real research work. Proposed fix (not built): persist partial section-workspace state with an incomplete/failed status marker before the terminal error, so a future run can still seed from it.

## Not Started / Mostly Planned

- DONE: Full local research-report loop/skill discoverability through `/loops`, `/skills`, and seed-loop catalog path (Workstream I, this session).
- PARTIAL: profile routing exists (`report_profiles.py` maps `report_type` → template/scoring for general/technical/deep_technical/market/policy/literature_review/competitive/product/academic). Missing: a first-class `ResearchConfig` intake object (topic/audience/purpose/depth/format/source_policy/output_policy) + frontend controls (Workstream D).
- REMAINING (large greenfield, Workstream J): catalog management for local/remote loops and skills — `/catalog/*` CRUD, enable/disable, copy-local, import/export, validation; plus remote source management (TTL/status/egress); plus frontend Find/Loops/Skills/Sources tabs. No `/catalog/*` routes exist yet.
- REMAINING (Workstream O / weak-model): deterministic report assembler as a full alternative path; JSONL-only weak-model finding contract. Current code relies on existing finding parsing + stricter prompts/budgets.

## Immediate Next Tasks

1. **DONE — final validation run completed cleanly.** Session `s_d89d406d5fbf` (Pi/Craft task,
   `task_hash=39ee3efddbd9`, auto-improve seeded from the clean cold-start `s_7be8460e09e7`)
   completed the full hill-climb: v1=0.22 (cold start) → v2=0.5434 → v3=0.6498 (`status: converged`,
   wall_s≈3325, 2.47M tokens). No connection-error crash (validates entry 165's concurrency/
   resilience fixes). Editor round 2 correctly REJECTED a non-improving change
   (`gate outcome: reject`, "round 2 regressed: score 0.650->0.650") and reverted — proves the
   revert-on-regression safety mechanism fires correctly on real output. Final artifact: title
   correctly derived (`# Study how to use Pi and Craft to develop agents and create a research`,
   entry 164 fix confirmed), zero duplicate section headings (entry 162 fix confirmed), no mermaid
   diagram present — expected and correct per entry 167's compliance checker: the task's
   requirement is "example code **or** design architecture" (an OR), and the artifact's code
   samples honestly satisfy it; a genuinely generic checker will not manufacture a diagram demand
   the task didn't make. Entries 161-167 collectively proven to interoperate cleanly in one live run.
2. Inspect dirty files before any commit (47+ files touched, none committed).
3. Run a fresh comparable cold E2E after restarting the backend/model path.
4. During that E2E, verify generated `io/*.in.md` includes full scoring, unresolved weaknesses, and fetched evidence paths.
5. Compare quality drift against `s_ab00296748df` and stronger prior baselines.
6. If live output remains shallow, focus next on evidence-use depth: fetched documents must drive analysis, summary, implications, limitations/reflection, not just citations.
7. Entry 166 (run-death-loses-all-progress) remains open — see Deferred section above.
